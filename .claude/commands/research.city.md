---
allowed-tools: Read, Write, Edit, Glob, Grep, WebSearch, WebFetch, Bash(mkdir:*), Bash(rm:*)
---

Исследуй город «$ARGUMENTS» и создай статью для базы знаний. Работай в три фазы.

Перед началом прочитай `structure.md` (правила документации) и `templates/city.md` (шаблон статьи). Ориентируйся на качество и глубину `travel-knowledge-base/uk/oxford.md`.

## Фаза 1: Генерация тем для исследования

Сгенерируй 50 поисковых тем, покрывающих все разделы шаблона `templates/city.md`:
- история города, ключевые даты и события
- экономика, производство, специалитеты
- главные достопримечательности (часы, цены, рейтинги)
- природа, парки, смотровые площадки
- активности (пантинг, велосипед, прогулки и т.п.)
- хидден джемс
- рестораны (разные кухни)
- пабы и бары (исторические и крафтовые)
- кофейни (независимые)
- рынки и стрит-фуд
- afternoon tea
- крафтовое пиво и micropubs
- воскресный ростбиф (Sunday Roast)
- вегетарианское и веганское
- кино и литература, связанные с городом
- известные уроженцы и выпускники
- архитектура и архитектурные стили
- исторические события и конфликты
- бесплатные активности
- посещение ключевых объектов (часы, цены)
- проживание (бюджетное и центральное)
- однодневная поездка vs ночёвка
- логистика (как добраться, транспорт внутри)

Сохрани список тем в `travel-knowledge-base/checklists/$ARGUMENTS.md` в формате чеклиста:
```
- [ ] Тема 1
- [ ] Тема 2
...
```

## Фаза 2: Исследование каждой темы

Для каждой темы из чеклиста:
1. Выполни веб-поиск по теме (в контексте города $ARGUMENTS).
2. Извлеки 1–3 наиболее содержательных результата.
3. Добавь найденные данные в `research-$ARGUMENTS/report.md` (append, не перезаписывай).
4. Отметь тему в чеклисте как `- [x]` (обнови файл `travel-knowledge-base/checklists/$ARGUMENTS.md`).

## Фаза 3: Компиляция статьи

После завершения всех тем:
1. Прочитай собранный `research-$ARGUMENTS/report.md`.
2. Скомпилируй данные в единую связную статью, следуя:
   - структуре из `templates/city.md` (порядок разделов фиксированный)
   - правилам из `structure.md` (русский язык, таблицы для мест с атрибутами, краткость)
3. Статья должна соответствовать по качеству и глубине `travel-knowledge-base/uk/oxford.md`.
4. Сохрани статью в `travel-knowledge-base/uk/$ARGUMENTS.md`.

## Фаза 4: Финализация

После сохранения статьи:
1. Удали все промежуточные файлы: удали директорию `research-$ARGUMENTS/` и всё её содержимое (`rm -rf research-$ARGUMENTS/`).
2. Обнови раздел «Разделы документации» в `structure.md`:
   - добавь строку `    - $ARGUMENTS.md` в секцию `- uk` в алфавитном порядке среди остальных файлов;
   - если секции `- checklists` ещё нет — добавь её после секции `- uk`; если есть — добавь строку `    - $ARGUMENTS.md` в алфавитном порядке.
3. Убедись, что в репозитории присутствуют оба файла и оба попадут в PR:
   - `travel-knowledge-base/uk/$ARGUMENTS.md` — статья о городе
   - `travel-knowledge-base/checklists/$ARGUMENTS.md` — чеклист поисковых запросов
